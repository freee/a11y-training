import {
  GoodPressed,
  BadStatusMessage,
} from '/src/components/examples/button';
import { ExampleContainer } from '/src/components/examples';

import { Title } from '/src/components/Title';

<Title>ステータスメッセージ</Title>

## ステータスメッセージ

### ステータスメッセージを自動的に読み上げる

#### 良い例: ボタンが押されたときにメッセージを自動的に読み上げる

<GoodPressed />

#### 悪い例: ボタンが押されたときにメッセージを自動的に読み上げない

<BadStatusMessage />
